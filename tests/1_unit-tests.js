/*
 *
 *
 *       FILL IN EACH UNIT TEST BELOW COMPLETELY
 *       -----[Keep the tests in the same order!]----
 *       (if additional are added, keep them at the very end!)
 */

// var chai = require("chai");
// var assert = chai.assert;
// var StockHandler = require("../controllers/stockHandler.js");

// var stockPrices = new StockHandler();

// suite("Unit Tests", function() {
//   suite("Function StockHandler.handleLikes", function() {
//     test("stock without like", async function() {
//       let stock = "GOOG";
//       let ip = "127.0.0.1";
//       let like = false;
//       let count = await stockPrices.handleLikes(stock, ip, like);
//       assert.equal(count, 0);
//     });
    
//     test("stock with like from 127.0.0.1 initial", async function() {
//       let stock = "GOOG";
//       let ip = "127.0.0.1";
//       let like = true;
//       let count = await stockPrices.handleLikes(stock, ip, like);
//       assert.equal(count, 1);
//     });
    
//     test("stock with like from 127.0.0.1 duplicate", async function() {
//       let stock = "GOOG";
//       let ip = "127.0.0.1";
//       let like = true;
//       let count = await stockPrices.handleLikes(stock, ip, like);
//       assert.equal(count, 1);
//     });
    
//     test("stock with like from 127.0.0.2", async function() {
//       let stock = "GOOG";
//       let ip = "127.0.0.2";
//       let like = true;
//       let count = await stockPrices.handleLikes(stock, ip, like);
//       assert.equal(count, 2);
//     }); 
//   });
// });
